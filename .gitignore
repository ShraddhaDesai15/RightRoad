node_modules/         # Prevents pushing heavy dependency folders
.env                  # Keeps sensitive Firebase & MongoDB keys private
dist/ or build/       # Ignores Vite/React build output
.vscode/              # Ignores VS Code workspace settings
.DS_Store             # macOS metadata file (safe to ignore)
